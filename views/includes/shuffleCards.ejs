<% if (likes.length > 0) { %>
<section id="shuffle-cards" class="lax">
  <% likes.forEach(m => { %>
  <div class="shuffle-cards_item">
    <img src="<%= m.poster %>" alt="Movie Poster" />
    <div class="shuffle-cards_item_actions">
      <a href="">unlike</a>
      <a href="">go</a>
    </div>
  </div>
  <% }) %>
</section>
<% } %>
